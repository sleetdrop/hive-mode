Basic Hive mode based on SQL mode.
Just does syntax highlighting.

To load hive-mode

1. Download the file EmacsHiveMode.lisp to local disk
2. load-file "Full path to EmacsHiveMode.lisp"
3. Open the hive file
4. M-x hive-mode

Or edit your .emacs file
